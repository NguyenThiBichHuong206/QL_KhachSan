<UserControl x:Class="QLKhachSan.views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Tên KH:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Width="150" Text="{Binding HoTen, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0" Padding="5"/>

            <TextBlock Text="SĐT:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Width="120" Text="{Binding SDT, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0" Padding="5"/>

            <TextBlock Text="CCCD:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Width="120" Text="{Binding CCCD, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0" Padding="5"/>

            <Button Content="Thêm" Command="{Binding AddCommand}" Width="70" Padding="5" Background="#10B981" Foreground="White" FontWeight="Bold" Margin="0,0,5,0" Cursor="Hand"/>
            <Button Content="Sửa" Command="{Binding UpdateCommand}" Width="70" Padding="5" Background="#3B82F6" Foreground="White" FontWeight="Bold" Margin="0,0,5,0" Cursor="Hand"/>
            <Button Content="Xóa (Ẩn)"
                    Command="{Binding DeleteCommand}"
                    Width="90"
                    Padding="5"
                    Background="#EF4444"
                    Foreground="White"
                    FontWeight="Bold"
                    Cursor="Hand"
                    Visibility="{Binding DeleteButtonVisibility}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding DanhSachKhachHang}" 
                  SelectedItem="{Binding SelectedCustomer}" 
                  AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã KH" Binding="{Binding MaKH}" Width="70"/>
                <DataGridTextColumn Header="Tên Khách Hàng" Binding="{Binding HoTen}" Width="*"/>
                <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SDT}" Width="150"/>
                <DataGridTextColumn Header="CCCD / CMND" Binding="{Binding CCCD}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>