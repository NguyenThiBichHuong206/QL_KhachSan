<Window x:Class="QLKhachsan.views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dang nhap he thong" Height="560" Width="900"
        MinHeight="520" MinWidth="820"
        WindowStartupLocation="CenterScreen"
        Background="#F4F7FB"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#146C94"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#0B3142"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#697586"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#DDE5EE"/>

        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,6,0,16"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="330"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PrimaryDarkBrush}" Padding="30">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="QL Khach San"
                           Foreground="White"
                           FontSize="30"
                           FontWeight="Bold"/>
                <TextBlock Text="Dang nhap de quan li phong, dat phong va hoa don."
                           Foreground="#B7D8E5"
                           TextWrapping="Wrap"
                           FontSize="15"
                           Margin="0,14,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1"
                Background="White"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="34"
                Margin="34">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,22">
                    <TextBlock Text="Tai khoan nguoi dung"
                               Foreground="#172033"
                               FontSize="26"
                               FontWeight="Bold"/>
                    <TextBlock Text="Dang nhap hoac tao tai khoan moi"
                               Foreground="{StaticResource MutedBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <TabControl Grid.Row="1">
                    <TabItem Header="Dang nhap">
                        <StackPanel Margin="0,22,0,0">
                            <TextBlock Text="Ten dang nhap" Foreground="#172033" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding LoginUsername, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource InputStyle}"/>

                            <TextBlock Text="Mat khau" Foreground="#172033" FontWeight="SemiBold"/>
                            <PasswordBox x:Name="LoginPasswordBox"
                                         PasswordChanged="LoginPasswordBox_PasswordChanged"
                                         Style="{StaticResource InputStyle}"/>

                            <Button Content="Dang nhap"
                                    Command="{Binding LoginCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </TabItem>

                    <TabItem Header="Dang ky">
                        <StackPanel Margin="0,22,0,0">
                            <TextBlock Text="Ten dang nhap" Foreground="#172033" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding RegisterUsername, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource InputStyle}"/>

                            <TextBlock Text="Mat khau" Foreground="#172033" FontWeight="SemiBold"/>
                            <PasswordBox x:Name="RegisterPasswordBox"
                                         PasswordChanged="RegisterPasswordBox_PasswordChanged"
                                         Style="{StaticResource InputStyle}"/>

                            <Button Content="Dang ky"
                                    Command="{Binding RegisterCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </TabItem>
                </TabControl>

                <TextBlock Grid.Row="2"
                           Text="{Binding Message}"
                           Foreground="#146C94"
                           TextWrapping="Wrap"
                           Margin="0,18,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
